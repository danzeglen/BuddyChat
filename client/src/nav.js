import React from 'react';
import {AppBar} from '@material-ui/core'

const Nav = () => {
    return (               
        <AppBar position='static'>
hi
        </AppBar>
    );
};

export default Nav;